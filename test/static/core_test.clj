(ns static.core-test
  (:require [clojure.test :refer :all]
            [b5v.core :refer :all])
  (:use [clojure.string :only [join split-lines]]))

(deftest core-test
  (testing "fix me"
    (is (= 0 1))))