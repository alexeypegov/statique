(ns statique.builder-test
  (:require [clojure.test :refer :all]
            [statique.builder :refer :all])
  (:use [clojure.string :only [join split-lines]]))

(deftest builder-test
  (testing "fix me"
    (is (= 1 1))))