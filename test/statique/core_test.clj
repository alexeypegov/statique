(ns statique.core-test
  (:require [clojure.test :refer :all]
            [statique.core :refer :all])
  (:use [clojure.string :only [join split-lines]]))

(deftest core-test
  (testing "fix me"
    (is (= 1 1))))